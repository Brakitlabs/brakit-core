#!/usr/bin/env node

// CLI entry point for brakit
// This file is the executable that gets linked when installed globally via npm
// It loads the compiled TypeScript CLI code from dist/cli/index.js
require("../dist/cli/index.js");
